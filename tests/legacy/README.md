# Legacy Root Tests

These tests were moved out of the repo root to reduce first-glance clutter.

They are still part of the regression surface, but they are older, narrower integration checks rather than the main architectural safety net.
